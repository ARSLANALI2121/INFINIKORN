<nav class="navbar navbar-expand-md navbar-light bg-light">
    <div class="container-fluid">
      <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="#"></a>
            </li>
            <% if user_signed_in? && current_user.admin? %>
              <li class="nav-item">
                <%= link_to 'Users', admin_users_path, class:"btn btn-info" %>||
                <%= link_to 'Client List', manager_clients_path, class:"btn btn-info"%>
              </li>
          <% end %>
          <% if user_signed_in? && current_user.manager? %>
          <li class="nav-item">
            <%= link_to 'Client List', manager_clients_path, class:"btn btn-info"%>
          </li>
          <% end %>
        </ul>
        <div class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
              <li><%= link_to 'Edit Profile', edit_user_registration_path , class:"btn btn-info"%></li>||
              <li><%= link_to "Log out", destroy_user_session_path, method: :delete, class:"btn btn-info" %></li>
            <% else %>
          <ul class="navbar-nav float-right">
              <li class="nav-item">
                <%= link_to "Log In", new_user_session_path , class:"btn btn-success"%>
          </ul>
          <% end %>
        </div> 
      </div>
    </div>
  </nav>
